#summary Checks whether the macro is defined in the [Variables] section

==!ExistMacro==

The command checks whether the macro is defined in the [Variables] section.

===Syntax===
{{{
If,ExistMacro,<Macro>,<Command>
}}}

===Note===
This command was added in Winbuilder 80; It will *not* work with older versions.